# 度小满数据加工 Notebook（hainanDXM.ipynb）运行环境清单
# 安装：pip install -r requirements.txt
# 说明：数据库连接信息统一读取 ../hainandxm.cnf（已加入 .gitignore，不进版本库），
#       notebook 内不落任何密码。
# 连接配置（必装）
pymysql>=2.2
pandas>=2.2
sqlalchemy>=1.4
# Jupyter 内核（必装）
ipykernel>=6.29
ipython-sql>=0.5   # 提供 %%sql magic，让代码 cell 能直接写 SQL
# 命令行执行/导出 notebook（可选）
# nbconvert>=7.16
# nbclient>=0.10
